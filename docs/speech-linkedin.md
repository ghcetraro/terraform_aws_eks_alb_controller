# Speech para LinkedIn — VPC + AWS Load Balancer Controller (EKS)

Documento listo para publicar. Pegá el link del repo en el **primer comentario** si querés maximizar alcance.

**Repo:** `https://github.com/ghcetraro/terraform_aws_eks_alb_controller`

---

## Post breve (con link)

```
Publiqué VPC + AWS Load Balancer Controller (EKS) — VPC etiquetada para EKS y AWS Load Balancer Controller — base de ingress en AWS

Open source · Terraform · AWS

🔗 https://github.com/ghcetraro/terraform_aws_eks_alb_controller

#eks #alb #terraform #aws #kubernetes #devops #opensource
```

**Alternativa** (link en primer comentario):

Post:
```
Publiqué VPC + AWS Load Balancer Controller (EKS) — VPC etiquetada para EKS y AWS Load Balancer Controller — base de ingress en AWS

Open source con Terraform. Link en el primer comentario 👇

#eks #alb #terraform #aws #kubernetes #devops #opensource
```

Comentario:
```
https://github.com/ghcetraro/terraform_aws_eks_alb_controller
```

---

## Post principal (recomendado)

```
La base que falta antes de publicar Ingress en EKS

En muchos equipos esto se resuelve a mano, distinto en cada cuenta, y se rompe en el próximo ambiente.

Por eso abrí VPC + AWS Load Balancer Controller (EKS):

Terraform en dos módulos: VPC con tags para EKS y despliegue del AWS Load Balancer Controller con IRSA.

Qué incluye:
- VPC: Tags listos para EKS / ALB discovery
- ALB Controller: Helm/Terraform sobre el cluster
- IRSA: Roles IAM vía OIDC del cluster
- Ejemplo: Carpeta `example/` de referencia
- Modular: `vpc/` + `alb-controller/`

Stack: Terraform · VPC · EKS · AWS Load Balancer Controller · IRSA

Todo con Terraform, ejemplos en tfvars.example (sin secretos en el repo).

¿Te sirve para tu plataforma? Feedback bienvenido.

Link del repo en el primer comentario 👇

#eks #alb #terraform #aws #kubernetes #devops #opensource
```

Comentario:
```
https://github.com/ghcetraro/terraform_aws_eks_alb_controller
```

---

## Versión corta / storytelling

```
Pasé demasiadas veces armando lo mismo en AWS/EKS “una vez más”.

Ahora está versionado: VPC + AWS Load Balancer Controller (EKS).

VPC etiquetada para EKS y AWS Load Balancer Controller — base de ingress en AWS

MIT · https://github.com/ghcetraro/terraform_aws_eks_alb_controller
```

---

## Primer comentario (engagement)

```
Repo + README + ejemplos: https://github.com/ghcetraro/terraform_aws_eks_alb_controller

Si lo usás o lo adaptás, contame qué mejorarías 🙌
```
